allprojects {
    repositories {
        jcenter()
    }
}

task publishPlugins {
    doLast {}
}

apply plugin: "java"
dependencies {
    runtime project(':orchidPlugin')
    runtime project(':orchidJavadoc')
}