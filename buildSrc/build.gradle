allprojects {
    repositories {
        jcenter()
    }
}

group = 'com.eden'

task publishPlugins {
    doLast {}
}

apply plugin: "java"
dependencies {
    runtime project(':orchidPlugin')
    runtime project(':orchidJavadocPlugin')
}
