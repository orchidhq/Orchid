tagPrefix = 'v'
autobump {
    majorPattern            = ~/^\[major\]/
    minorPattern            = ~/^\[minor\]/
    patchPattern            = ~/^\[patch\]/
    newPreReleasePattern    = ~/^\[pre-release\]/
    promoteToReleasePattern = ~/^\[release\]/
}