{%- set path = link.path -%}
{%- set pages = index | query(path) -%}
{%- if pages -%}
    {%- import 'macros/nav.twig' as _nav_ -%}

    {%- set pages = pages | walk('url') -%}

    {{- _nav_.createSiteLink(link, pages) -}}
{%- endif -%}

