{% extends 'includes/termArchive-group-baseLinked' %}

{% block cardBody %}
{% set 'previewImage' = page.get('images')|first %}
<img class="img-responsive"
     style="width: 100%; height: 200px; object-fit: cover;" src="{{ previewImage.src }}&blur"
     alt="{{ previewImage.alt }}">
{% endblock %}
