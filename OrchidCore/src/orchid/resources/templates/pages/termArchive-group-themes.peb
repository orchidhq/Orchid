{% extends 'includes/termArchive-group-baseLinked' %}

{% block cardBody %}
<div class="plugin-preview">
    <img class="img-responsive" src="{{ page.get('thumbnail') }}" alt="{{ page.title }}">
</div>
{% endblock %}
