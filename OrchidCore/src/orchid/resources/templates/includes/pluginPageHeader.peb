<div>

  <h1 class="is-size-1 title">{{ page.title }}</h1>
  <p class="is-size-5 subtitle">{{ page.description }}</p>

{#  githubRepo, siteUrl #}

  {# desktop stuff #}
  <div class="tags">
  {% for tag in page.tags %}
    <span class="tag {{ (tag == 'official') ? 'is-primary' : 'is-light' }}">{{ tag }}</span>
  {% endfor %}
  </div>
  <hr>
</div>
