{% set 'parser' = controller.provide(classType) %}
{# @pebvariable name="parser" type="com.eden.orchid.api.compilers.OrchidParser" #}

<p><code>{{ parser.priority }}: [{{ parser.sourceExtensions | join('</code>, <code>') }}]</code></p>
<p><code>{{ parser.delimiterString | raw }}</code></p>