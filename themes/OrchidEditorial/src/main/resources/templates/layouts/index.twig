<!DOCTYPE HTML>
<!--
    Editorial by HTML5 UP
    html5up.net | @ajlkn
    Free for personal and commercial use under the CCA 3.0 license (html5up.net/license)
-->
<html>
<head>
    {% include 'includes/head.twig' %}
    {% include 'includes/styles.twig' with {page: page} only %}
    {% include 'meta.twig' ignore missing %}
</head>
<body class="is-loading">

<!-- Wrapper -->
<div id="wrapper">

    <!-- Main -->
    <div id="main">
        <div class="inner">

            <!-- Header -->
            {% block innerHeader %}
                {% include 'includes/header.twig' %}
            {% endblock %}
            {% block banner %}
            {% endblock %}
            {% block pageContent %}
                <section id="pageContent">
                    <header class="major"><h2>{{ page.title }}</h2></header>
                    <article>
                        {% include 'includes/componentHolder.twig' with {componentHolder: page.components} %}
                    </article>
                </section>
            {% endblock %}
        </div>
        <hr>

        <!-- Page Sidebar -->
        {% include 'includes/footer.twig' %}
    </div>

    <!-- Sidebar -->
    {% include 'includes/sidebar.twig' %}
</div>

{% include 'includes/scripts.twig' with {page: page} only %}

</body>
</html>