<label
        class="checkbox"
        for="{{ form.key }}--{{ field.key }}">
  <input
          type="{{ field.type }}"
          name="{{ field.key }}"
          id="{{ form.key }}--{{ field.key }}"
          {% if field.required %}required{% endif %}
          {% if field.default %}checked{% endif %}>
  {{ field.label }}
</label>