package com.eden.orchid.api.events

/**
 * Denotes a class that contains callbacks annotated with [On] to be registered as individual Event listeners.
 * Must be registered in the Module to the Set [OrchidEventListener].
 *
 * @since v1.0.0
 */
interface OrchidEventListener
